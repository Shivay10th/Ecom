/** @format */

import React, { Link } from 'react';
import Base from '../core/Base';

const Signin = () => {
	return (
		<Base title="Sign In page" description="User can sign in here.">
			<h1>sign in page working</h1>
		</Base>
	);
};

export default Signin;
